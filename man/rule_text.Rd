% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{rule_text}
\alias{rule_text}
\title{Convert a rule expression to a readable text format}
\usage{
rule_text(
  expr,
  bullets = FALSE,
  digits = 4,
  max_width = Inf,
  key = NULL,
  max_group_nchar = Inf
)
}
\arguments{
\item{expr}{An R expression to format. Typically created by
\code{\link[=rect_split_to_expr]{rect_split_to_expr()}} or \code{\link[=combine_rule_elements]{combine_rule_elements()}}.}

\item{bullets}{Logical indicating whether to break apart rule elements and
display as a bulleted list. If \code{TRUE}, splits on \code{&} operators and creates
a bulleted list with each condition on a new line. If \code{FALSE} (default),
returns as a single line.}

\item{digits}{Integer number of significant digits to use when formatting
numeric values in the rule. Default is 4.}

\item{max_width}{Maximum width for the output when \code{bullets = FALSE}. If the
formatted rule exceeds this width, it will be truncated with \code{..}
appended. The \code{..} is included in the width count. Default is \code{Inf} (no
truncation).}

\item{key}{Optional data frame or tibble with columns \code{original} and \code{label}
(both character). When provided, variable names matching values in
\code{original} are substituted with corresponding values in \code{label} in the
printed output. The \code{original} column must not contain duplicates. If a
variable name is not in \code{key$original}, it remains unchanged.}

\item{max_group_nchar}{Maximum number of characters for value lists in \code{\%in\%}
operations. When the total character count of values in a \code{c(...)} vector
exceeds this limit, the values are replaced with \verb{\{X values\}} where X is
the count. Default is \code{Inf} (no abbreviation). Character count includes
quotes and separators as they appear in deparsed output.}
}
\value{
A character string containing the formatted rule. When
\code{bullets = TRUE}, conditions are separated by newlines with bullet markers.
}
\description{
This function formats R expressions representing rules from tree-based models
as character strings. It provides options for formatting numeric values,
displaying rules as bulleted lists, and controlling output width.
}
\examples{
# Simple numeric rule
rule1 <- rlang::expr(age >= 30)
rule_text(rule1)

# Multiple conditions
rule2 <- rlang::expr(age >= 30 & income > 50000)
rule_text(rule2)

# Bulleted format
cat(rule_text(rule2, bullets = TRUE), "\n")

# Control numeric precision
rule3 <- rlang::expr(x > 1.23456789)
rule_text(rule3, digits = 2)
rule_text(rule3, digits = 6)

# Truncate long rules
rule4 <- rlang::expr(very_long_variable_name > 100 & another_long_name < 50)
rule_text(rule4, max_width = 30)

# With label substitution
expr <- rlang::expr(pct_owed > 0.5 & amount < 1000)
key <- tibble::tibble(
  original = c("pct_owed", "amount"),
  label = c("percentage owed by customer", "total amount")
)
rule_text(expr, key = key)

# Integration with other helpers
split1 <- list(column = "age", value = 30.5, operator = ">=")
split2 <- list(column = "income", value = 50000, operator = ">")
split3 <- list(column = "city", value = c("NYC", "LA"), operator = "\%in\%")
rule <- combine_rule_elements(list(
  rect_split_to_expr(split1),
  rect_split_to_expr(split2),
  rect_split_to_expr(split3)
))
cat(rule_text(rule, bullets = TRUE), "\n")

# Abbreviate long value lists
split4 <- list(
  column = "county",
  value = c("adams", "benton", "chelan", "clallam"),
  operator = "\%in\%"
)
rule_long <- rect_split_to_expr(split4)
rule_text(rule_long) # Full list
rule_text(rule_long, max_group_nchar = 20) # Abbreviated

}
