% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/C5.0.R
\name{as.party.C5.0}
\alias{as.party.C5.0}
\title{Convert C5.0 model to party object}
\usage{
\method{as.party}{C5.0}(obj, tree = 1L, data = NULL, ...)
}
\arguments{
\item{obj}{A \code{C5.0} object from the \pkg{C50} package.}

\item{tree}{Integer specifying which tree to convert (1-based indexing,
default is 1). For single tree models, use \code{tree = 1}. For boosted models
with \code{trials > 1}, this selects which boosting iteration to extract.}

\item{data}{Data.frame containing the training data, including both predictors
and response variable. Required for proper party object creation with fitted
values and node summaries.}

\item{...}{Not currently used.}
}
\value{
A \code{party} object from the \pkg{partykit} package.
}
\description{
Convert a single tree from a C5.0 decision tree or boosted model to a party
object for use with partykit visualization and analysis tools.
}
\details{
\subsection{C5.0 tree storage format}{

The \pkg{C50} package stores trees in a custom text format in \code{obj$tree}. This
format uses indented lines with key-value pairs:
\itemize{
\item \code{type="2"}: Internal node with split
\item \code{type="0"}: Terminal/leaf node
\item \code{att="VariableName"}: Attribute/variable to split on
\item \code{forks="3"}: Number of branches (typically 2 for binary, 3 for ternary)
\item \code{cut="threshold"}: Numeric threshold for split
\item \code{class="ClassName"}: Predicted class
\item \code{freq="n1,n2,n3"}: Frequency of each class at node
}
}

\subsection{Boosting and trials}{
\itemize{
\item Single tree models (\code{trials = 1}): Only \code{tree = 1} is valid
\item Boosted models (\code{trials > 1}): Multiple sequential trees available
\item The \code{tree} parameter maps to trial/iteration number
\item Each boosting trial produces one tree
}
}

\subsection{Tree structure}{
\itemize{
\item Trees use indentation to indicate hierarchy (similar to Python)
\item Child nodes are indented relative to parent
\item Ternary splits: <= threshold, missing, > threshold
\item Binary splits: <= threshold vs > threshold
}
}

\subsection{Split encoding}{
\itemize{
\item For numeric: typically left/low when <= threshold, right/high when > threshold
\item C5.0 may use ternary splits with separate handling of missing values
\item \pkg{partykit} uses binary splits, so we simplify to binary when needed
}
}

\subsection{Variable names}{
\itemize{
\item \code{obj$predictors} contains ordered list of predictor variable names
\item \code{att} attribute in tree text references these by name
\item Map to 1-based indices for \pkg{partykit}
}
}

\subsection{Important limitations}{
\itemize{
\item \pkg{C50}'s text format is complex and may vary by version
\item Ternary splits with missing value branches simplified to binary
\item Rule-based models (\code{obj$rules != ""}) not supported
\item Categorical variable splits may need special handling
}
}
}
\examples{
if (rlang::is_installed(c("C50", "palmerpenguins"))) {
  data(penguins, package = "palmerpenguins")
  penguins <- na.omit(penguins)

  # Single tree model
  set.seed(2847)
  c5_tree <- C50::C5.0(species ~ ., data = penguins)
  party_tree <- as.party(c5_tree, tree = 1L, data = penguins)
  print(party_tree)
  plot(party_tree)

  # Boosted model with multiple trials
  set.seed(5193)
  c5_boost <- C50::C5.0(species ~ ., data = penguins, trials = 3)
  # Extract first boosting iteration
  party_tree1 <- as.party(c5_boost, tree = 1L, data = penguins)
  # Extract third boosting iteration
  party_tree3 <- as.party(c5_boost, tree = 3L, data = penguins)
}

}
