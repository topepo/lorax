% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grf.R
\name{as.party.regression_forest}
\alias{as.party.regression_forest}
\alias{as.party.grf}
\title{Convert grf model to party object}
\usage{
\method{as.party}{regression_forest}(obj, tree = 1L, data = NULL, ...)

\method{as.party}{grf}(obj, tree = 1L, data = NULL, ...)
}
\arguments{
\item{obj}{A grf object (e.g., \code{regression_forest}, \code{causal_forest}) from
the grf package.}

\item{tree}{Integer specifying which tree to convert (1-based indexing,
default is 1). Must be between 1 and the number of trees in the forest.}

\item{data}{Optional data.frame containing the training data. If NULL,
will attempt to extract from the grf object (\code{obj$X.orig}), or create a
placeholder data.frame. Providing data enables full party functionality.}

\item{...}{Not currently used.}
}
\value{
A \code{party} object from the partykit package.
}
\description{
Convert a single tree from a grf (generalized random forests) model to a
party object for use with partykit visualization and analysis tools.
}
\details{
\subsection{GRF tree storage format}{

The grf package stores trees in a nested list structure, typically accessed
via \code{grf::get_tree(obj, tree)}. Each tree is represented as nested lists:
\itemize{
\item \code{is_leaf}: Logical, TRUE for terminal nodes
\item \code{split_variable}: 0-based index of variable to split on (internal nodes)
\item \code{split_value}: Numeric threshold for split (internal nodes)
\item \code{left_child}: Nested list for left subtree (internal nodes)
\item \code{right_child}: Nested list for right subtree (internal nodes)
\item Leaf nodes contain prediction information
}
}

\subsection{Node indexing}{
\itemize{
\item Internally, grf uses 0-based variable indices
\item User-facing \code{tree} parameter uses 1-based indexing (R convention)
\item Trees use 0-based indexing internally but we access with 1-based tree number
}
}

\subsection{Split encoding}{
\itemize{
\item For numeric variables: left child when feature < threshold, right child
when feature >= threshold
\item partykit split created with \code{right = TRUE} (right interval closed)
}
}

\subsection{Tree structure}{
\itemize{
\item grf provides nested list structure (not flattened)
\item This is the most direct representation for recursive conversion
\item Each node is a list with is_leaf flag and split info
}

The party object will use 1-based node IDs and variable indices as required
by partykit.
}
}
\examples{
\dontrun{
library(grf)

# Regression forest
rf <- regression_forest(X = penguins[, ], Y = penguins$bill_length_mm, num.trees = 10)

# Convert first tree
party_tree <- as.party(rf, tree = 1)
print(party_tree)
plot(party_tree)

# Can also work with other grf forest types
cf <- causal_forest(X = penguins[, 3:6], Y = penguins$bill_length_mm,
                    W = rbinom(150, 1, 0.5), num.trees = 10)
party_tree2 <- as.party(cf, tree = 1)
}

}
