% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/randomForest.R
\name{as.party.randomForest}
\alias{as.party.randomForest}
\title{Convert randomForest model to party object}
\usage{
\method{as.party}{randomForest}(obj, tree = 1L, data = NULL, ...)
}
\arguments{
\item{obj}{A \code{randomForest} object from the \pkg{randomForest} package.}

\item{tree}{Integer specifying which tree to convert (1-based indexing,
default is 1). Must be between 1 and the number of trees in the forest.}

\item{data}{Optional data.frame containing the training data. If NULL,
a placeholder data.frame will be created with correct variable names but
no observations. Providing data enables full party functionality including
predictions.}

\item{...}{Not currently used.}
}
\value{
A \code{party} object from the \pkg{partykit} package.
}
\description{
Convert a single tree from a randomForest model to a party object for use
with partykit visualization and analysis tools.
}
\details{
\subsection{randomForest tree storage format}{

The randomForest package stores trees in \code{obj$forest} as parallel matrices:
\itemize{
\item \code{leftDaughter[i, tree]}: 1-based row index of left child (0 = no child)
\item \code{rightDaughter[i, tree]}: 1-based row index of right child (0 = no child)
\item \code{bestvar[i, tree]}: 1-based variable index for split (0 for terminal)
\item \code{xbestsplit[i, tree]}: threshold value for split
\item \code{nodestatus[i, tree]}: node status (-1 = terminal, -3 = internal)
\item \code{nodepred[i, tree]}: prediction at node (for regression) or class (classification)
}
}

\subsection{Node indexing}{
\itemize{
\item randomForest uses 1-based row indices for nodes (root is row 1)
\item Value 0 in leftDaughter/rightDaughter indicates no child
\item User-facing \code{tree} parameter uses 1-based indexing (R convention)
}
}

\subsection{Split encoding}{
\itemize{
\item For numeric variables: left child when feature <= threshold, right child
when feature > threshold
\item Note: randomForest uses <= for left (different from ranger's <)
\item partykit split created with \code{right = FALSE} to match this
}
}

\subsection{Terminal node identification}{
\itemize{
\item nodestatus == -1 indicates terminal node
\item Alternatively: bestvar == 0 or both daughters == 0
}

The party object will use 1-based node IDs and variable indices as required
by partykit.
}
}
\examples{
if (rlang::is_installed(c("randomForest", "palmerpenguins"))) {
  data(penguins, package = "palmerpenguins")
  penguins <- na.omit(penguins)

  rf <- randomForest::randomForest(species ~ ., data = penguins, ntree = 10)

  # Convert first tree
  party_tree <- as.party(rf, tree = 1L, data = penguins)
  print(party_tree)
  plot(party_tree)

  # Predictions from party object
  predict(party_tree, newdata = penguins[1:5, ])
}

}
