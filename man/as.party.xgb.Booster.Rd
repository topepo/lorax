% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xgb.Booster.R
\name{as.party.xgb.Booster}
\alias{as.party.xgb.Booster}
\title{Convert xgb.Booster model to party object}
\usage{
\method{as.party}{xgb.Booster}(obj, tree = 1L, data, ...)
}
\arguments{
\item{obj}{An \code{xgb.Booster} object from the xgboost package.}

\item{tree}{Integer specifying which tree to convert (1-based indexing,
default is 1). For multiclass models with \code{num_class} classes and
\code{nrounds} boosting rounds, there are \code{num_class * nrounds} total trees.}

\item{data}{data.frame containing the training data \strong{with the response
variable included} (required). XGBoost models do not store the original
training data or response values. You must provide the original data frame
that includes both the predictor variables and the response variable.}

\item{...}{Not currently used.}
}
\value{
A \code{constparty} object from the partykit package.
}
\description{
Convert a single tree from an XGBoost boosted tree model to a party object
for use with partykit visualization and analysis tools.
}
\details{
\subsection{Important note on data}{

XGBoost models do not store the original training data or response values.
You \strong{must} provide the original data frame (including the response variable)
via the \code{data} parameter for correct terminal node statistics, bar charts,
and other visualizations.
}

\subsection{XGBoost tree storage format}{

XGBoost stores trees in a tabular format accessible via
\code{xgboost::xgb.model.dt.tree()}. Each tree is represented as rows in a table:
\itemize{
\item \code{Tree}: 0-based tree index (e.g., 0, 1, 2, ...)
\item \code{Node}: 0-based node ID within tree (e.g., "0-0", "0-1" for tree 0)
\item \code{Feature}: Feature name (character) or "Leaf" for terminal nodes
\item \code{Split}: Numeric threshold for splits (NA for leaves)
\item \code{Yes}: 0-based node ID of yes branch (feature < threshold)
\item \code{No}: 0-based node ID of no branch (feature >= threshold)
\item \code{Missing}: 0-based node ID for missing values
\item \code{Quality}: Prediction value for leaf nodes, gain for internal nodes
}
}

\subsection{Node indexing}{
\itemize{
\item Internally, XGBoost uses 0-based tree and node indices
\item User-facing \code{tree} parameter uses 1-based indexing (R convention)
\item When tree=1 is requested, we filter to Tree==0 internally
}
}

\subsection{Split encoding}{
\itemize{
\item Yes branch: feature < threshold (left child)
\item No branch: feature >= threshold (right child)
\item partykit split created with \code{right = TRUE} (right interval closed)
}
}

\subsection{Child node references}{
\itemize{
\item Yes column: node ID for left child (< condition)
\item No column: node ID for right child (>= condition)
\item Leaf nodes have Feature == "Leaf"
}
}

\subsection{Variable names}{
\itemize{
\item Feature column contains actual feature names (not indices)
\item Must map to column positions in data.frame
\item If numeric indices used (f0, f1, ...), map to data columns
}

The party object will use 1-based node IDs and variable indices as required
by partykit.
}
}
\examples{
\dontrun{
library(xgboost)

data(agaricus.train, package = "xgboost")
dtrain <- xgb.DMatrix(agaricus.train$data, label = agaricus.train$label)

bst <- xgb.train(
  data = dtrain,
  max_depth = 3,
  nrounds = 5,
  objective = "binary:logistic"
)

# Convert first tree
party_tree <- as.party(bst, tree = 1)
print(party_tree)
plot(party_tree)
}

}
