% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranger.R
\name{as.party.ranger}
\alias{as.party.ranger}
\title{Convert ranger model to party object}
\usage{
\method{as.party}{ranger}(obj, tree = 1L, data = NULL, ...)
}
\arguments{
\item{obj}{A \code{ranger} object from the ranger package.}

\item{tree}{Integer specifying which tree to convert (1-based indexing,
default is 1). Must be between 1 and the number of trees in the forest.}

\item{data}{Optional data.frame containing the training data. If NULL,
a placeholder data.frame will be created with correct variable names but
no observations. Providing data enables full party functionality including
predictions.}

\item{...}{Not currently used.}
}
\value{
A \code{party} object from the partykit package.
}
\description{
Convert a single tree from a ranger random forest model to a party object
for use with partykit visualization and analysis tools.
}
\details{
\subsection{Ranger tree storage format}{

The ranger package stores trees in \code{obj$forest} with parallel vectors:
\itemize{
\item \code{split.varIDs[[tree]]}: 0-based variable indices for splits
\item \code{split.values[[tree]]}: threshold values for splits
\item \code{child.nodeIDs[[tree]]}: matrix with 2 columns (left, right child IDs)
\item \code{is.ordered[[tree]]}: whether split variable is ordered (for categoricals)
\item All node IDs are 0-based (root = 0)
}
}

\subsection{Node indexing}{
\itemize{
\item Internally, ranger uses 0-based node indices (root is node 0)
\item User-facing \code{tree} parameter uses 1-based indexing (R convention)
\item Leaf nodes have split.varIDs entry of NA or large sentinel value
}
}

\subsection{Split encoding}{
\itemize{
\item For numeric variables: left child when feature < threshold, right child
when feature >= threshold
\item partykit split created with \code{right = TRUE} (right interval closed)
}
}

\subsection{Child node references}{
\itemize{
\item \code{child.nodeIDs} is a matrix with 2 columns: left_child, right_child
\item Value 0 indicates no child (terminal node)
\item Both children 0 means current node is terminal
}

The party object will use 1-based node IDs and variable indices as required
by partykit.
}
}
\examples{
if (rlang::is_installed(c("ranger", "palmerpenguins"))) {
  data(penguins, package = "palmerpenguins")
  penguins <- na.omit(penguins)

  rf <- ranger::ranger(species ~ ., data = penguins, num.trees = 10)

  # Convert first tree
  party_tree <- as.party(rf, tree = 1, data = penguins)
  print(party_tree)
  plot(party_tree)

  # Predictions from party object
  predict(party_tree, newdata = penguins[1:5, ])
}

}
