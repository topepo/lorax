% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lgb.Booster.R
\name{as.party.lgb.Booster}
\alias{as.party.lgb.Booster}
\title{Convert lgb.Booster model to party object}
\usage{
\method{as.party}{lgb.Booster}(obj, tree = 1L, data, ...)
}
\arguments{
\item{obj}{An \code{lgb.Booster} object from the lightgbm package.}

\item{tree}{Integer specifying which tree to convert (1-based indexing,
default is 1). For multiclass models with \code{num_class} classes and
\code{nrounds} boosting rounds, there are \code{num_class * nrounds} total trees.}

\item{data}{data.frame containing the training data \strong{with the response
variable included} (required). LightGBM models do not store the original
training data or response values. You must provide the original data frame
that includes both the predictor variables and the response variable.}

\item{...}{Not currently used.}
}
\value{
A \code{constparty} object from the partykit package.
}
\description{
Convert a single tree from a LightGBM boosted tree model to a party object
for use with partykit visualization and analysis tools.
}
\details{
\subsection{Important note on data}{

LightGBM models do not store the original training data or response values.
You \strong{must} provide the original data frame (including the response variable)
via the \code{data} parameter for correct terminal node statistics, bar charts,
and other visualizations.
}

\subsection{LightGBM tree storage format}{

LightGBM stores trees in a tabular format accessible via
\code{lightgbm::lgb.model.dt.tree()}. Each tree is represented as rows in a table:
\itemize{
\item \code{tree_index}: 0-based tree index
\item \code{split_index}: 0-based node ID for internal nodes (NA for leaves)
\item \code{leaf_index}: 0-based node ID for leaf nodes (NA for internal)
\item \code{split_feature}: Feature name (character) for splits
\item \code{threshold}: Numeric threshold for splits
\item \code{decision_type}: Split type ("<=", "==", etc.)
\item \code{left_child}: 0-based node ID of left child
\item \code{right_child}: 0-based node ID of right child
\item \code{leaf_value}: Prediction value for leaf nodes
\item \code{node_parent}: 0-based parent node ID
\item \code{depth}: Depth of node in tree
}
}

\subsection{Node indexing}{
\itemize{
\item Internally, LightGBM uses 0-based tree and node indices
\item User-facing \code{tree} parameter uses 1-based indexing (R convention)
\item When tree=1 is requested, we filter to tree_index==0 internally
\item Internal nodes use split_index, leaf nodes use leaf_index
}
}

\subsection{Split encoding}{
\itemize{
\item decision_type "<=": left child when feature <= threshold
\item right child when feature > threshold
\item partykit split created with \code{right = FALSE} (left interval closed)
}
}

\subsection{Child node references}{
\itemize{
\item Internal nodes have explicit left_child and right_child IDs
\item These reference either split_index (internal) or leaf_index (leaf)
\item Need to look up child in appropriate column based on node type
}
}

\subsection{Variable names}{
\itemize{
\item split_feature column contains actual feature names or "Column_N" defaults
\item Must map to column positions in data.frame
}

The party object will use 1-based node IDs and variable indices as required
by partykit.
}
}
\examples{
\dontrun{
library(lightgbm)

data(agaricus.train, package = "lightgbm")
dtrain <- lgb.Dataset(agaricus.train$data, label = agaricus.train$label)

bst <- lgb.train(
  params = list(objective = "binary", max_depth = 3),
  data = dtrain,
  nrounds = 5
)

# Convert first tree
party_tree <- as.party(bst, tree = 1)
print(party_tree)
plot(party_tree)
}

}
