% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ObliqueForest.R
\name{oblique_split_to_scaled_expr}
\alias{oblique_split_to_scaled_expr}
\title{Extract rules from an ObliqueForest model}
\usage{
oblique_split_to_scaled_expr(split_info, x)
}
\arguments{
\item{x}{An \code{ObliqueForest} object from the \pkg{aorsf} package}

\item{tree}{Integer specifying which tree to extract rules from (1-based).
Default is \code{1L} for the first tree. Must be between 1 and the number of
trees in the forest (\code{x$n_tree}).}

\item{node_id}{An integer for the node ID to process.}
}
\value{
A tibble with columns:
\itemize{
\item \code{tree}: integer, the tree number (1-based).
\item \code{rules}: list of R expressions, one per terminal node.
\item \code{id}: integer, the terminal node ID (1-based for user convenience).
}
}
\description{
Extracts the decision rules for terminal nodes in a specified tree from an
aorsf ObliqueForest model. Each rule represents the path from the root node
to a terminal node using oblique (linear combination) splits.
}
\details{
\subsection{Tree and Node Indexing}{

Both the \code{tree} parameter and the \code{id} column use \strong{1-based indexing} for
user convenience, matching R's standard indexing convention:
\itemize{
\item \code{tree = 1} extracts rules from the first tree
\item \code{id = 1} refers to the first terminal node
}

Internally, aorsf uses 0-based indexing (where node 0 is the root), but this
is automatically converted to 1-based indexing in the output for consistency
with R conventions.
}

\subsection{Factor Variables and Reference Coding}{

The \pkg{aorsf} package internally converts unordered factor variables using
\strong{reference coding} (also called dummy coding). For a factor with k levels,
aorsf creates k-1 binary indicator variables, with the first level serving as
the reference category:
\itemize{
\item A factor \code{color} with levels \verb{["red", "blue", "green"]} creates indicators
for \code{blue} and \code{green} only. When both indicators are 0, it represents \code{red}.
\item The extracted rules automatically convert these back to factor comparisons:
\code{2.1 * color_blue} becomes \code{2.1 * (color == "blue")}.
\item Rules can be evaluated directly on data with the original factor columns
(no need to manually create indicator variables).
\item Ordered factors are converted to a single integer variable representing the
ordinal level, not to multiple indicators.
}

Reference coding prevents collinearity in the internal regression computations
used to find optimal splits.
}

\subsection{Predictor Scaling}{

The \pkg{aorsf} package \strong{always scales data during prediction}, regardless of
the \code{scale_x} parameter setting. The coefficients stored in trees are for
scaled data: \code{(x - mean) / sd} for numeric predictors.

To make rules work with unscaled input data, the extracted rules automatically
\strong{include the scaling transformation} in the expressions themselves. For example,
instead of showing a pre-computed unscaled coefficient, rules show:

\if{html}{\out{<div class="sourceCode">}}\preformatted{728.58 * ((flipper_length_mm - 200.97) / 14.02) > 400.23
}\if{html}{\out{</div>}}

This approach:
\itemize{
\item Allows rules to be evaluated directly on original (unscaled) data
\item Preserves full floating-point precision (avoids errors from pre-computing
unscaled coefficients)
\item Makes the scaling transformation explicit and transparent
}

Factor indicator variables are not scaled since they are binary 0/1 values.
}
}
\examples{
if (rlang::is_installed(c("aorsf", "palmerpenguins"))) {
  # Classification example
  penguins <- palmerpenguins::penguins[complete.cases(palmerpenguins::penguins), ]
  set.seed(2847)
  forest <- aorsf::orsf(species ~ ., data = penguins, n_tree = 3)

  # Extract rules from first tree (default)
  rules <- extract_rules(forest)

  # View rules as text
  rules$rules[[1]] |> rule_text(bullets = TRUE) |> cat("\n")

  # Extract rules from different tree
  rules3 <- extract_rules(forest, tree = 3L)

  # Regression example
  data(mtcars)
  set.seed(5193)
  forest_reg <- aorsf::orsf(mpg ~ ., data = mtcars, n_tree = 3)
  rules_reg <- extract_rules(forest_reg, tree = 1L)
}

}
