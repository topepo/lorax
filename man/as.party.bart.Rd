% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bart.R
\name{as.party.bart}
\alias{as.party.bart}
\title{Convert BART model to party object}
\usage{
\method{as.party}{bart}(obj, tree = 1L, chain = 1L, data, ...)
}
\arguments{
\item{obj}{A \code{bart} object from the dbarts package fitted with
\code{keeptrees = TRUE}.}

\item{tree}{Integer specifying which tree to convert (1-based indexing,
default is 1). BART models contain \code{n.trees} trees in the ensemble.}

\item{chain}{Integer specifying which MCMC chain to extract from (1-based
indexing, default is 1). Only relevant for models fitted with multiple
chains.}

\item{data}{data.frame containing the \strong{original untransformed} training
data with original response values (required). BART internally transforms
data (creating dummy variables for factors and converting responses to
0/1). You must provide the original data frame that includes both the
predictor variables and the response variable in their original formats
(e.g., factors for classification).}

\item{...}{Not currently used.}
}
\value{
A \code{constparty} object from the partykit package.
}
\description{
Convert a single tree from a BART (Bayesian Additive Regression Trees) model
to a party object for use with partykit visualization and analysis tools.
}
\details{
\subsection{Important note on data transformation}{

BART internally transforms the training data in ways that make it unsuitable
for display in party objects. Specifically, BART creates dummy variables for
factor predictors and converts factor responses to 0/1 numeric values. To get
correct terminal node statistics, bar charts, and other visualizations, you
\strong{must} provide the original untransformed data (including the response
variable) via the \code{data} parameter.
}

\subsection{BART tree storage format}{

The dbarts package stores trees in depth-first traversal order in a
data.frame accessible via \code{obj$fit$getTrees()}. Each row represents one node:
\itemize{
\item \code{var}: 1-based variable index for split, or -1 for terminal nodes
\item \code{value}: threshold for internal nodes, prediction for terminal nodes
\item \code{tree}: 1-based tree number
\item \code{chain}: chain number (if multiple chains)
\item \code{sample}: MCMC sample number
}
}

\subsection{Depth-first traversal order}{
\itemize{
\item Nodes stored as: parent, left subtree (complete), right subtree (complete)
\item Example: root at row 1, left child at row 2, right child after left subtree
\item Must track row consumption to determine subtree boundaries
}
}

\subsection{Node indexing}{
\itemize{
\item User-facing \code{tree} and \code{chain} parameters use 1-based indexing (R convention)
\item Variable indices in \code{var} column are 1-based (match obj$varNames)
\item Value -1 in \code{var} indicates terminal node
}
}

\subsection{Split encoding}{
\itemize{
\item Left child: feature < threshold
\item Right child: feature >= threshold
\item partykit split created with \code{right = TRUE} (right interval closed)
}
}

\subsection{Variable names}{
\itemize{
\item Available in \code{obj$fit$data@x} column names or \code{obj$varNames}
\item \code{var} column provides 1-based index into these names
}

The party object will use 1-based node IDs and variable indices as required
by partykit.
}
}
\examples{
if (rlang::is_installed(c("dbarts", "palmerpenguins"))) {
 # Prepare data with response column
 train_data <- penguins[, c("bill_length_mm", "bill_depth_mm",
                            "flipper_length_mm", "body_mass_g", "species")]

 fit <- bart(
  x.train = train_data[, 1:4],
  y.train = train_data$species,
  keeptrees = TRUE,
  verbose = FALSE,
  ntree = 2
 )

 # Convert first tree - data parameter is required
 # Response will be preserved in original format (e.g., factor for classification)
 party_tree <- as.party(fit, tree = 1, chain = 1, data = train_data)
 print(party_tree)
 plot(party_tree)
}
\dontrun{
}

}
