% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ObliqueForest.R
\name{extract_rules.ObliqueForest}
\alias{extract_rules.ObliqueForest}
\title{Extract rules from an ObliqueForest model}
\usage{
\method{extract_rules}{ObliqueForest}(x, tree = 1L, ...)
}
\arguments{
\item{x}{An \code{ObliqueForest} object from the aorsf package}

\item{tree}{Integer specifying which tree to extract rules from (1-based).
Default is \code{1L} for the first tree. Must be between 1 and the number of
trees in the forest (\code{x$n_tree}).}

\item{...}{Not currently used}
}
\value{
A tibble with columns:
\itemize{
\item \code{tree}: integer, the tree number (1-based)
\item \code{rules}: list of R expressions, one per terminal node
\item \code{id}: integer, the terminal node ID (1-based for user convenience)
}
}
\description{
Extracts the decision rules for terminal nodes in a specified tree from an
aorsf ObliqueForest model. Each rule represents the path from the root node
to a terminal node using oblique (linear combination) splits.
}
\details{
\subsection{Tree and Node Indexing}{

Both the \code{tree} parameter and the \code{id} column use \strong{1-based indexing} for
user convenience, matching R's standard indexing convention:
\itemize{
\item \code{tree = 1} extracts rules from the first tree
\item \code{id = 1} refers to the first terminal node
}

Internally, aorsf uses 0-based indexing (where node 0 is the root), but this
is automatically converted to 1-based indexing in the output for consistency
with R conventions.
}

\subsection{Factor Variables and One-Hot Encoding}{

The aorsf package internally converts unordered factor variables to binary
indicator (dummy) variables. This means:
\itemize{
\item For an unordered factor with \code{k} levels, \code{k} indicator variables are created
(one for each level, not k-1 as in reference coding)
\item Indicator variables are named as \verb{\{variable_name\}_\{level_name\}}
\item Each indicator is 1 when the observation has that level, 0 otherwise
\item For example, a factor \code{color} with levels \verb{["red", "blue", "green"]} creates
indicator variables \code{color_red}, \code{color_blue}, and \code{color_green}
}

Ordered factors are converted to a single integer variable representing the
ordinal level, not to multiple indicators.

As a result, the variable names in extracted rules may differ from the
original data frame column names. Rules will reference the indicator variables
(e.g., \code{color_red}, \code{color_blue}, \code{color_green}) rather than the original
factor column (\code{color}).
}

\subsection{Predictor Scaling and Unscaling}{

The aorsf package can optionally center and scale numeric predictors when
computing linear combinations for splits. This is controlled by the \code{scale_x}
parameter in \verb{orsf_control_*()} functions (default is \code{TRUE}).

When \code{scale_x = TRUE}, aorsf uses \code{(x - mean) / sd} for numeric predictors
during split computations. The extracted rules automatically \strong{unscale} the
coefficients and thresholds back to the original units, so rules can be
directly evaluated on the original (unscaled) data.

When \code{scale_x = FALSE}, coefficients are already in the original units and
no unscaling is performed.

Factor indicator variables (from one-hot encoding) are not scaled since they
are binary 0/1 values.

Note: \code{x$get_means()} and \code{x$get_stdev()} always return the data statistics
regardless of the \code{scale_x} setting. To determine if scaling was used, check
\code{x$control$lincomb_scale}.
}
}
\examples{
\dontrun{
library(aorsf)
penguins <- palmerpenguins::penguins[complete.cases(palmerpenguins::penguins), ]
forest <- orsf(species ~ ., data = penguins, n_tree = 10)

# Extract rules from first tree (default)
rules <- extract_rules(forest)

# View rules as text
rules$rules[[1]] |> rule_text(bullets = TRUE) |> cat("\n")

# Extract rules from different tree
rules5 <- extract_rules(forest, tree = 5)
}

}
