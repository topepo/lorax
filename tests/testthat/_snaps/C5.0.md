# as.party.C5.0 validates tree parameter

    Code
      lorax::as.party(c5_model, tree = 0, data = penguins)
    Condition
      Error in `lorax::as.party()`:
      ! `tree` must be >= 1, not 0.

---

    Code
      lorax::as.party(c5_model, tree = 5, data = penguins)
    Condition
      Warning:
      `tree` = 5 exceeds number of trials (1). Using `tree` = 1 instead.
    Output
      
      Model formula:
      species ~ bill_length_mm + bill_depth_mm + flipper_length_mm + 
          body_mass_g
      
      Fitted party:
      [1] root
      |   [2] flipper_length_mm < 206: Adelie (n = 213, err = 30.0%)
      |   [3] flipper_length_mm >= 206
      |   |   [4] bill_depth_mm < 17.6: Gentoo (n = 122, err = 0.0%)
      |   |   [5] bill_depth_mm >= 17.6
      |   |   |   [6] bill_length_mm < 46.5: Adelie (n = 2, err = 0.0%)
      |   |   |   [7] bill_length_mm >= 46.5: Chinstrap (n = 5, err = 0.0%)
      
      Number of inner nodes:    3
      Number of terminal nodes: 4

---

    Code
      lorax::as.party(c5_model, tree = c(1, 2), data = penguins)
    Condition
      Error in `lorax::as.party()`:
      ! `tree` must be a single integer, not a double vector.

---

    Code
      lorax::as.party(c5_model, tree = "1", data = penguins)
    Condition
      Error in `lorax::as.party()`:
      ! `tree` must be a single integer, not a string.

# as.party.C5.0 requires data parameter

    Code
      lorax::as.party(c5_model, tree = 1)
    Condition
      Error in `lorax::as.party()`:
      ! `data` is required for `as.party.C5.0()`.

# as.party.C5.0 works with boosted models

    Code
      lorax::as.party(c5_boost, tree = 0, data = penguins)
    Condition
      Error in `lorax::as.party()`:
      ! `tree` must be >= 1, not 0.

---

    Code
      lorax::as.party(c5_boost, tree = 11, data = penguins)
    Condition
      Warning:
      `tree` = 11 exceeds number of trials (10). Using `tree` = 10 instead.
    Output
      
      Model formula:
      species ~ bill_length_mm + bill_depth_mm + flipper_length_mm + 
          body_mass_g
      
      Fitted party:
      [1] root
      |   [2] body_mass_g < 4800: Adelie (n = 261, err = 42.1%)
      |   [3] body_mass_g >= 4800: Gentoo (n = 81, err = 0.0%)
      
      Number of inner nodes:    1
      Number of terminal nodes: 2

# as.party.C5.0 rejects rule-based models

    Code
      lorax::as.party(c5_rules, tree = 1, data = penguins)
    Condition
      Error in `lorax::as.party()`:
      ! C50 rule-based models are not supported. Use tree-based models only.

# extract_rules.C5.0() validates tree argument

    Code
      extract_rules(c5_boost, tree = "1", data = wa_trees)
    Condition
      Error:
      ! `tree` must be an integer vector, not a string.

---

    Code
      extract_rules(c5_boost, tree = 1.5, data = wa_trees)
    Condition
      Error:
      ! `tree` must be an integer vector, not a number.

---

    Code
      extract_rules(c5_boost, tree = 0L, data = wa_trees)
    Condition
      Error:
      ! `tree` values must be between 1 and 5.

---

    Code
      extract_rules(c5_boost, tree = 10L, data = wa_trees)
    Condition
      Error:
      ! `tree` values must be between 1 and 5.

# extract_rules.C5.0() validates tree argument for rule models

    Code
      extract_rules(c5_rules, tree = 0L, data = data)
    Condition
      Error:
      ! `tree` values must be between 1 and 1.

---

    Code
      extract_rules(c5_rules, tree = 10L, data = data)
    Condition
      Error:
      ! `tree` values must be between 1 and 1.

# active_predictors.C5.0() validates tree argument

    Code
      lorax::active_predictors(fit, tree = "1")
    Condition
      Error:
      ! `tree` must be an integer vector, not a string.

---

    Code
      lorax::active_predictors(fit, tree = 1.5)
    Condition
      Error:
      ! `tree` must be an integer vector, not a number.

---

    Code
      lorax::active_predictors(fit, tree = 0L)
    Condition
      Error:
      ! `tree` values must be between 1 and 3, not 0.

---

    Code
      lorax::active_predictors(fit, tree = 10L)
    Condition
      Error:
      ! `tree` values must be between 1 and 3, not 10.

